import Gists from './models/gists';
import File from './models/file';
import User from './models/user';

export const gists = new Gists();

export const file = new File();

export const user = new User();
